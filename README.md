
Live Demo URL: https://lumina-health.netlify.app/

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies: `npm install`
2. Set the `VITE_SUPABASE_URL`, `VITE_SUPABASE_ANON_KEY` and `APP_URL`
3. Run the app: `npm run dev`
